<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="proshandadmin.OrdersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:proshandadmin"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <TextBlock x:Name="pageTitle" Text="Manage Orders" Margin="16" Style="{StaticResource TitleTextBlockStyle}"/>

            <Grid Margin="16"  >
                <StackPanel Orientation="Horizontal" Spacing="60" HorizontalAlignment="Left" Padding="16" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">

                <TextBlock Text="Order Id" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Text="First Name" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Text="Last Name" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Text="Email" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Text="Mobile" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Text="Address" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Text="Hand Size" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Text="Hand Color" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBlock Text="Status" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
